<style>
{% if section.settings.font_variant == 'custom' %}
    {%- liquid
      assign body_font_bold = section.settings.title_font | font_modify: 'weight', 'bold'
      assign body_font_italic = section.settings.title_font | font_modify: 'style', 'italic'
      assign body_font_bold_italic = body_font_bold | font_modify: 'style', 'italic'
    %}
    {{ section.settings.title_font | font_face: font_display: 'swap' }}
    {{ body_font_bold | font_face: font_display: 'swap' }}
    {{ body_font_italic | font_face: font_display: 'swap' }}
    {{ body_font_bold_italic | font_face: font_display: 'swap' }}
    {{ settings.type_header_font | font_face: font_display: 'swap' }}
    #shopify-section-{{section.id}} * {
      font-family: {{ section.settings.title_font.family }};
    }
  {% endif %}
  /* Dynamic styles — unchanged */
#shopify-section-{{section.id}} .material-symbols-outlined {
  /* Icon variation settings */
  font-variation-settings:
    'FILL' 0,
    'wght' 400,
    'GRAD' 0,
    'opsz' 48;
  /* Icon size */
  font-size: 24px;
  /* Align with text vertically */
  vertical-align: middle;
}

#shopify-section-{{section.id}} {
  display: {% case section.settings.display_on %}
            {% when 'all' %} block;
            {% when 'mobile_only' %} none;
            {% when 'desktop_only' %} block;
          {% endcase %};
  background-color: {{ section.settings.background_color }};
  text-align: {{ section.settings.heading_align }};
  margin: 0 auto;
  position: relative;
  overflow: hidden;
  width: 100%;
}

#shopify-section-{{section.id}} .content-wrapper {
  margin: 0 auto;
  max-width: {% if section.settings.section_width == 'full' %}100%{% else %}{{ settings.page_width }}px{% endif %};
  padding: {{ section.settings.padding_y_desk }}px {{ section.settings.padding_x_desk }}px;
  position: relative;
  text-align: {{ section.settings.heading_align }};
}

#shopify-section-{{section.id}} .heading {
  max-width: 50%;
}

#shopify-section-{{section.id}} .title * {
  margin: 0;
}

#shopify-section-{{section.id}} .title h2,
#shopify-section-{{section.id}} .title h1,
#shopify-section-{{section.id}} .title h3,
#shopify-section-{{section.id}} .title p {
  font-size: {% case section.settings.heading_size %}
               {% when 'small' %}36px
               {% when 'medium' %}45px
               {% when 'large' %}60px
             {% endcase %};
  text-transform: {{ section.settings.headline_typography }};
  color: {{ section.settings.title_color }};
  margin: 0;
  text-align: {% case section.settings.heading_align %}
                {% when 'left' %}left;
                {% when 'center' %}center;
                {% when 'right' %}right;
              {% endcase %};
  line-height: 110%;
  font-weight: 700;
  margin-bottom: 40px;
}

#shopify-section-{{section.id}} .subtitle * {
  margin: 0;
}

#shopify-section-{{section.id}} .subtitle h2,
#shopify-section-{{section.id}} .subtitle h3,
#shopify-section-{{section.id}} .subtitle p {
  color: {{ section.settings.subtitle_color }};
  font-size: 20px;
  font-weight: 400;
  text-align: {% case section.settings.heading_align %}
                {% when 'left' %}left;
                {% when 'center' %}center;
                {% when 'right' %}right;
              {% endcase %};
  line-height: 150%;
  margin: 0;
  margin-bottom: 32px;
}

#shopify-section-{{section.id}} ul li {
  margin-bottom: 26.5px;
  display: flex;
  align-items: center;
  gap: 10px;
  width: 100%;
}

.tab-button {
  background: #fff;
  border: none;
  padding: 0 20px;
  margin: 0;
  cursor: pointer;
  font-size: 22px;
  font-weight: bold;
  color: {{ section.settings.tabs_text_color }};
  height: 91px;
  /* Center vertically */
  line-height: 91px;
  position: relative;
  flex: 1;
  text-align: center;
  border-bottom: 2px solid #ddd;
  display: flex;
  align-items: center;
  justify-content: center;
}

.tab-button.active {
  background: {{ section.settings.tabs_color }};
  color: {{ section.settings.tabs_texta_color }};
}

.tab-button.active::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-top: 10px solid {{ section.settings.tabs_color }};
}

.tab-image img,
.tab-image .placeholder-image {
  width: 100%;
  height: 460px;
  background: #ccc;
  border-radius: {{ section.settings.image_round }}px;
  object-fit: cover;
}

.tab-text {
  padding-top: 40px;
  padding-bottom: 40px;
  padding-left: 80px;
  color: {{ section.settings.body_color }};
}

/* Mobile responsive dynamic styles */
@media (max-width: 768px) {
  #shopify-section-{{section.id}} {
    display: {% case section.settings.display_on %}
                {% when 'all' %}block;
                {% when 'mobile_only' %}block;
                {% when 'desktop_only' %}none;
              {% endcase %};
  }

  #shopify-section-{{section.id}} .content-wrapper {
    max-width: 100%;
    padding: {{ section.settings.padding_y_mob }}px {{ section.settings.padding_x_mob }}px;
  }

  #shopify-section-{{section.id}} .heading {
    width: 100%;
    max-width: 100%;
  }

  #shopify-section-{{section.id}} .title h2,
  #shopify-section-{{section.id}} .title h1,
  #shopify-section-{{section.id}} .title h3,
  #shopify-section-{{section.id}} .title p {
    font-size: {% case section.settings.heading_size %}
                 {% when 'small' %}20px
                 {% when 'medium' %}28px
                 {% when 'large' %}36px
               {% endcase %};
    line-height: 100%;
    margin: 0;
    margin-bottom: 24px;
  }

  #shopify-section-{{section.id}} .subtitle h2,
  #shopify-section-{{section.id}} .subtitle h3,
  #shopify-section-{{section.id}} .subtitle p {
    font-size: 14px;
    margin-bottom: 24px;
  }

  #shopify-section-{{section.id}} ul li {
    margin-bottom: 16px;
  }

  .tab-button.active::after {
    bottom: -8px;
    border-left: 8px solid transparent;
    border-right: 8px solid transparent;
    border-top: 8px solid {{ section.settings.tabs_color }};
  }
}

</style>


{% schema %}
{
  "name": "Tabbed Content",
  "class": "tabs",
  "settings": [
      {
      "type": "header",
      "content": "General Settings"
    },
    { "type": "color", "id": "background_color", "label": "Background color", "default": "transparent" },
      { "type": "select", "id": "font_variant", "label": "Font Family", "options": [{ "value": "default", "label": "Default by theme" }, { "value": "custom", "label": "Own choice" }], "default": "default" },
    { "type": "font_picker", "label": "Regular Font", "id": "title_font", "default": "montserrat_n4" },

      { "type": "color", "id": "title_color", "label": "Title Color", "default": "#000" },
    { "type": "color", "id": "subtitle_color", "label": "Subtitle Color", "default": "#666" },
      { "type": "color", "id": "body_color", "label": "Body Text Color", "default": "#000" },
    { 
      "type": "select", 
      "id": "heading_size", 
      "label": "Heading Size", 
      "options": [{ "value": "small", "label": "Small" }, { "value": "medium", "label": "Medium" }, { "value": "large", "label": "Large" }], 
      "default": "medium" 
    },
    {
      "type": "select", 
      "id": "headline_typography", 
      "label": "Headline Typography", 
      "options": [
        { "value": "uppercase", "label": "AA" }, 
        { "value": "capitalize", "label": "Aa" }, 
        { "value": "lowercase", "label": "aa" }
      ], 
      "default": "uppercase" 
    },
    { 
      "type": "select", 
      "id": "heading_align", 
      "label": "Heading Alignment", 
      "options": [
        { "value": "left", "label": "Left" }, 
        { "value": "center", "label": "Center" }, 
        { "value": "right", "label": "Right" }
      ], 
      "default": "center" 
    },
    { 
      "type": "select", 
      "id": "section_width", 
      "label": "Section Width", 
      "options": [
        { "value": "full", "label": "Full Width" }, 
        { "value": "contained", "label": "Contained" }
      ], 
      "default": "contained" 
    },
    { "type": "range", "id": "padding_x_desk", "label": "Horizontal Padding", "info": "for desktop", "min": 0, "max": 100, "default": 0 },
    { "type": "range", "id": "padding_y_desk", "label": "Vertical Padding", "info": "for desktop", "min": 0, "max": 100, "default": 50 },
    { "type": "range", "id": "padding_x_mob", "label": "Horizontal Padding", "info": "for mobile", "min": 0, "max": 100, "default": 20 },
    { "type": "range", "id": "padding_y_mob", "label": "Vertical Padding", "info": "for mobile", "min": 0, "max": 100, "default": 20 },
    { 
      "type": "select", 
      "id": "display_on", 
      "label": "Display On", 
      "options": [
        { "value": "all", "label": "All Devices" }, 
        { "value": "mobile_only", "label": "Mobile Only" }, 
        { "value": "desktop_only", "label": "Desktop Only" }
      ], 
      "default": "all" 
    },
     {
     "type": "range",
      "id": "image_round",
      "label": "Image Round Corner",
      "min": 0, "max": 50, "step": 5, "default": 10 
    },
        { "type": "color", "id": "tabs_color", "label": "Tab Caption Color", "default": "#000" },
      { "type": "color", "id": "tabs_texta_color", "label": "Tab Caption Active Text Color", "default": "#fff" },
      { "type": "color", "id": "tabs_text_color", "label": "Tab Caption Text Color", "default": "#000" }

  
  ],
  "blocks": [
    {
      "type": "tab",
      "name": "Tab",
      "settings": [
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title",
          "default": "Content Tab 1"
        },
        {
          "type": "image_picker",
          "id": "tab_image",
          "label": "Tab Image"
        },
      
    {
      "type": "header",
      "content": "Content Settings"
    },
        {
          "type": "richtext",
          "id": "title",
          "label": "Title",
          "default": "<h2>DUIS AUTE IRURE DOLOR IN REPREHENDERIT IN</h2>"
        },
        {
          "type": "richtext",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "<h3>Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. sint culpa qui officia deserunt mollit anim id est laborum</h3>"
        },
        {
          "type": "header",
          "content": "Icons Settings"
        },
        {
          "type": "image_picker",
          "id": "icon_1",
          "label": "Icon Image For Point 1"
        },
        {
          "type": "text",
          "id": "icon_1_text",
          "label": "Icon Symbol For Point 1",
        },
        {
          "type": "text",
          "id": "point_1_text",
          "label": "Text For Point 1",
          "default": "Perfect for gym, yoga & sunday strolls"
        },
        {
          "type": "image_picker",
          "id": "icon_2",
          "label": "Icon Image For Point 2"
        },
        {
          "type": "text",
          "id": "icon_2_text",
          "label": "Icon Symbol For Point 2"
        },
        {
          "type": "text",
          "id": "point_2_text",
          "label": "Text for Point 2",
          "default": "Perfect for gym, yoga & sunday strolls"
        },
        {
          "type": "image_picker",
          "id": "icon_3",
          "label": "Icon Image For Point 3"
        },
        {
          "type": "text",
          "id": "icon_3_text",
          "label": "Icon Symbol For Point 3"
        },
        {
          "type": "text",
          "id": "point_3_text",
          "label": "Text For Point 3",
          "default": "Perfect for gym, yoga & sunday strolls"
        },
      ]
    }
  ],
  "presets": [
    {
      "name": "Tabbed Content",
      "category": "Info",
      "blocks": [
        {
          "type": "tab"
        },
        {
          "type": "tab"
        },
        {
          "type": "tab"
        }
      ]
    }
  ]
}
{% endschema %}

<div class="content-wrapper">
  <div class="tabs-container">
    {% for block in section.blocks %}
      <button class="tab-button {% if forloop.first %}active{% endif %}" data-tab="{{ block.id }}">{{ block.settings.tab_title }}</button>
    {% endfor %}
  </div>
  <div class="tab-content">
    {% for block in section.blocks %}
      <div class="tab-pane {% if forloop.first %}active{% endif %}" data-tab="{{ block.id }}">
        <div class="tab-image">
          {% if block.settings.tab_image %}
            <img src="{{ block.settings.tab_image | img_url: 'large' }}" alt="{{ block.settings.tab_title }}">
          {% else %}
            <div class="placeholder-image"></div>
          {% endif %}
        </div>
        <div class="tab-text heading">
         <div class="title">   {{ block.settings.title }}  </div>
          <div class="subtitle">    {{ block.settings.subtitle }} </div>
          <ul class="image-with-text-list">
            <li>
              {% if block.settings.icon_1 != blank %}
                <img src="{{ block.settings.icon_1 | img_url: '24x24' }}" alt="Icon 1">
              {% else %}
                {% if block.settings.icon_1_text != blank %}
              <span class="material-symbols-outlined">     {{ block.settings.icon_1_text }}</span>
                {% else %}
                  {% render 'icon_number_1' %}
                {% endif %}
              {% endif %}
              {{ block.settings.point_1_text }}
            </li>
            <li>
              {% if block.settings.icon_2 != blank %}
                <img src="{{ block.settings.icon_2 | img_url: '24x24' }}" alt="Icon 2">
              {% else %}
                {% if block.settings.icon_2_text != blank %}
                  <span class="material-symbols-outlined"> {{ block.settings.icon_2_text }}</span>
                {% else %}
                  {% render 'icon_number_2' %}
                {% endif %}
              {% endif %}
              {{ block.settings.point_2_text }}
            </li>
            <li>
              {% if block.settings.icon_3 != blank %}
                <img src="{{ block.settings.icon_3 | img_url: '24x24' }}" alt="Icon 3">
              {% else %}
                {% if block.settings.icon_3_text != blank %}
                  <span class="material-symbols-outlined">{{ block.settings.icon_3_text }}</span>
                {% else %}
                  {% render 'icon_number_3' %}
                {% endif %}
              {% endif %}
              {{ block.settings.point_3_text }}
            </li>
          </ul>
        </div>
      </div>
    {% endfor %}
  </div>
</div>



<script>
document.addEventListener('DOMContentLoaded', function() {
  const section = document.querySelector('.tabs');
  const tabButtons = section.querySelectorAll('.tab-button');
  const tabPanes = section.querySelectorAll('.tab-pane');

  tabButtons.forEach(button => {
    button.addEventListener('click', () => {
      const tabId = button.getAttribute('data-tab');

      // Remove active class from all buttons and panes
      tabButtons.forEach(btn => btn.classList.remove('active'));
      tabPanes.forEach(pane => pane.classList.remove('active'));

      // Add active class to clicked button and corresponding pane
      button.classList.add('active');
      section.querySelector(`.tab-pane[data-tab="${tabId}"]`).classList.add('active');
    });
  });
});
</script>